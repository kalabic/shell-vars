Test is to be run on Linux/Unix with a dot '.', like this:
. ./test_path.sh

It will create dummy 'user_shell_paths' in current folder, but also change 'PATH' variable, so re-login to reset these changes they should not be permanent.
